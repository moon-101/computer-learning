{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Run Current File",
        "type": "shell",
        "command": "python",
        "args": [
          "${file}"
        ],
        "options": {
          "cwd": "${fileDirname}"
        },
        "problemMatcher": [],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ]
  }