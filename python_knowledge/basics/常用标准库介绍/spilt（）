split() 函数是 Python 字符串对象的一个内置方法，用于将字符串分割成一个列表。这个方法可以根据指定的分隔符将字符串拆分成多个部分，并返回这些部分组成的列表。如果没有指定分隔符，默认使用任何空白字符（如空格、制表符、换行符等）作为分隔符。

以下是 split() 函数的基本用法和一些示例：

基本用法
str.split(separator, maxsplit)
CopyInsert
separator（可选）：指定分隔符，默认为任何空白字符。
maxsplit（可选）：指定最大分割次数，默认为 -1，表示不限制分割次数。